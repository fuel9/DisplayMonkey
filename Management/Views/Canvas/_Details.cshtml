@model DisplayMonkey.Models.Canvas

<fieldset>
    <legend>Canvas</legend>
        <table><tr>
        <td>

    @Html.HiddenFor(p => p.CanvasId)

    <div class="divTable">
    <div class="divRow">
    <div class="display-field">
        @Html.Label(Model.Name)
        @Html.DisplayFor(model => model.Width) x @Html.DisplayFor(model => model.Height) pixels
    </div>
    </div>
    </div>
        </td>
        <td class="preview">
            <div id="canvas" data-path="@Html.Raw(Url.Action("Data", "Canvas"))">
                <div>
                    <img src="@Html.Raw(Url.Action("Thumb", "Media", new { id="nnn", width="qqq", height="qqq" }))" alt="Background" />
                </div>
            </div>
        </td>
    </tr></table>
</fieldset>
